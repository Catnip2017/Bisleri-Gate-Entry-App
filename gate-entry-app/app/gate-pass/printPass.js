// app/gate-pass/printPass.js - DEPRECATED STUB, SAFE TO DELETE.
// The print logic moved to utils/printGatePass.js: files under app/ are
// routes to Expo Router, and a route's default export must be a React
// component — the old async-function export crashed the router.
// This stub only exists because the session couldn't delete files; it renders
// nothing if someone ever navigates to /gate-pass/printPass.
export default function PrintPassDeprecatedRoute() {
  return null;
}
